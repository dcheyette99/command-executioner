Rewriting a tool in python that was written in bash. 

V1 goals:

Take an input file (commands)
Take a host file (routers/switches)

Use the tool to execute the commands on the routers and send the output to text files. 
